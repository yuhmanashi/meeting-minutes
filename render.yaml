databases:
  - name: meeting_minutes
    user: meeting_minutes
    plan: free
    region: oregon

services:
  - type: web
    name: meeting_minutes
    plan: free
    region: oregon
    branch: main
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: meeting_minutes
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false